function [z]=uniforminv(p,param);% uniforminv                - inverse uniform cumulative distribution function (Jan 1,2001)%% Compute the quantiles for a uniform distribution with% specified lower and upper bounds parameters. %% SYNTAX :%% [z]=uniforminv(p,param);%% INPUT :%% p        n by k   matrix of values for the cumulative probabilities.% param    1 by 2   parameters of the uniform distribution, where :%                   param(1) is the lower bound of the distribution,%                   param(2) is the upper bound of the distribution,%                   with param(1)<=param(2).%% OUTPUT :%% z        n by k   matrix of quantile values computed at the%                   corresponding p values.%% NOTE :%% The interval [param(1),param(2)] is supposed to be closed.a=param(1);b=param(2);isvalid=(a<=b);if isvalid==0,  error('parameters are not valid');end;if max(p(:))>1 | min(p(:))<0,  error('cumulative probability values must be inside [0,1]');end;z=p*(b-a)+a;