function [pdf]=uniformpdf(z,param);% uniformpdf                - uniform probability distribution function (Jan 1,2001)%% Compute the values of the probability density% function for a uniform distribution with% specified lower and upper bounds parameters.%% SYNTAX :%% [pdf]=uniformpdf(z,param);%% INPUT :%% z        n by k   matrix of values for which the probability%                   distribution function must be computed.% param    1 by 2   parameters of the uniform distribution, where :%                   param(1) is the lower bound of the distribution,%                   param(2) is the upper bound of the distribution,%                   with param(1)<=param(2).%% OUTPUT :%% pdf      n by k   matrix of values for the probability distribution%                   function computed at the corresponding z values.%% NOTE :%% The interval [param(1),param(2)] is supposed to be closed,% so that pdf is different from 0 at z=param(1) and z=param(2).a=param(1);b=param(2);isvalid=(a<=b);if isvalid==0,  error('parameters are not valid');end;if (a==b),  pdf=zeros(size(z));  index=find(z==a);  pdf(index)=Inf;else  isinside=(z>=a)&(z<=b);  pdf=isinside/(b-a);end;