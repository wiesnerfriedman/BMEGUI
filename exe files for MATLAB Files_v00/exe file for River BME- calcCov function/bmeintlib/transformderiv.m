function [dg]=transformderiv(yfile,cdfyfile);% transformderiv            - derivative of a transform function (Jan 1,2001)%% Compute the derivative of the transform function that% maps the zero-mean unit variance Gaussian distribution% to a specified arbitrary distribution using a numerical% finite difference scheme.%% SYNTAX :%% [dg]=transformderiv(yfile,cdfyfile);%% INPUT :%% yfile     n by 1   vector of values sorted in ascending order.% cdfyfile  n by 1   vector of values of the cumulative distribution%                    function associated with the yfile values.%% OUPUT :%% dg        n by 1   vector of the derivative of the transform at the%                    yfile values.zfile=gaussinv(cdfyfile,[0 1]);forward=diff(yfile)./diff(zfile);n=length(yfile);dg=zeros(n,1);dg(1)=forward(1);dg(n)=forward(n-1);dg(2:n-1)=0.5*forward(2:n-1)+0.5*forward(1:n-2);