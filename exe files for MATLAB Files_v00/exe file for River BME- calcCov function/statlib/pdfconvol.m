function [pdfc]=pdfconvol(z,pdf,param);% pdfconvol                 - convolution of a pdf with a Gaussian pdf (Jan 1,2001)%% Compute the convolution between an arbitrary probability% distribution function and a Gaussian probability % distribution function.%% SYNTAX :%% [pdfc]=pdfconvol(z,pdf,param); %% INPUT :%% z           n by 1   vector of values sorted in ascending order. % pdf         n by 1   vector of values for the probability distribution%                      function to be convoluted at the z values with the%                      Gaussian distribution.% param       1 by 2   vector of parameters for the Gaussian probability%                      distribution function, where :%                      param(1) is the mean of the distribution,%                      param(2) is the variance of the distribution.%% OUTPUT :%% pdfc        n by 1   vector of the values for the convoluted distributions%                      at the z values. n=length(z);pdfc=zeros(n,1);for i=1:n  product=gausspdf(z(i)-z,param).*pdf;  pdfc(i)=trapezint(z,product,min(z),max(z));end;